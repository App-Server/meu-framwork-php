RewriteEngine On

# Não redireciona se for um arquivo ou pasta real (css, js, imagens)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Manda tudo para o index.php passando o parâmetro 'url'
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]